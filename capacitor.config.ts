import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elite72.designintelligence',
  appName: 'Elite 72 | Design Intelligence',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
